# QA MCP Server — environment configuration
# Copy to .env and fill in real values.

# QA1 MySQL — shared team credentials
# BASE_URL must end with a trailing slash; schema name is appended per-request.
#
# host.docker.internal reaches the host-machine MySQL from inside Docker Desktop (macOS/Windows).
# On Linux: add 'extra_hosts: ["host.docker.internal:host-gateway"]' under mcp-server in docker-compose.yml
BASE_URL=mysql+aiomysql://FILL_IN_USER:FILL_IN_PASSWORD@host.docker.internal:3307/

# Symbol index — built by mcp-indexed-tabs/index_repo.sh, mounted from ./data/
SYMBOL_INDEX_PATH=/data/symbol_index.db

# MCP server host/port
# Default port 8765 (chosen to avoid conflicts with common services on 8000/8080/8001).
MCP_HOST=0.0.0.0
MCP_PORT=8765

# Per-route port overrides (optional).
# Leave commented for single-port deployment (both routes share MCP_PORT).
# Uncomment and set different values to split routes onto separate ports in the future.
# DB_MCP_PORT=8765
# SYMBOLS_MCP_PORT=8766
